# Floor-Plan Studio — copy to app/.env and fill in (app/.env is gitignored; never commit keys)

# P3 geometry-ops agent loop — default provider (spec decision D5)
DEEPSEEK_API_KEY=sk-...

# P4 vision ingestion + quality escalation (required for image → geometry; D5)
ANTHROPIC_API_KEY=sk-ant-...

# P5 rent comparables web search (search provider swappable)
TAVILY_API_KEY=tvly-...
